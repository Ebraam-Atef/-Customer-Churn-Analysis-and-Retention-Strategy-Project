# Customer Churn Prediction — Requirements
# Install with: pip install -r requirements.txt

# ── Existing ML stack (unchanged) ─────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=1.7.0
imbalanced-learn>=0.11.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0
streamlit>=1.31.0

# ── SHAP — per-customer (local) model explanations ─────────────────
# Powers app.py's Key Factors panel via shap.TreeExplainer (exact Tree
# SHAP for XGBoost / Random Forest / Gradient Boosting). Required as of
# the audit pass that replaced the global-importance heuristic.
shap>=0.45.0

# SHAP — required for customer-specific (local) "Key Factors" explanation.
# Replaces the old global feature_importances_-based heuristic in app.py's
# compute_shap_factors(). TreeExplainer gives exact, fast, signed
# attributions for tree models (RandomForest / XGBoost / GradientBoosting).
shap>=0.45.0

# ── LangChain stack ────────────────────────────────────────────────
# Core LangChain — LCEL chains, prompt templates, output parsers
langchain>=0.2.16

# Text splitters — separate package as of LangChain 1.x
# (previously bundled inside langchain.text_splitter)
langchain-text-splitters>=1.0.0

# Ollama integration — ChatOllama + OllamaEmbeddings (fully local, no API key)
# Replaces langchain-anthropic; requires Ollama running on localhost:11434
langchain-ollama>=0.1.0

# Community loaders and FAISS integration
langchain-community>=0.2.16

# FAISS vector store (CPU build — swap faiss-gpu for CUDA machines)
faiss-cpu>=1.8.0

# PDF document loading
pypdf>=4.3.0

# .env file support
python-dotenv>=1.0.0
